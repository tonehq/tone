apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tone-call-certificate
  namespace: dev
spec:
  secretName: tone-call-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: dev-call.trytone.ai
  dnsNames:
  - dev-call.trytone.ai
  duration: 2160h # 90 days
  renewBefore: 720h # 30 days
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
