apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: staging-certificate
  namespace: staging
spec:
  secretName: staging-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: staging.trytone.ai
  dnsNames:
  - staging.trytone.ai
  duration: 2160h # 90 days
  renewBefore: 720h # 30 days
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
