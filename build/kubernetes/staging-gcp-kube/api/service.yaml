apiVersion: v1
kind: Service
metadata:
  name: staging-gcp-kube-tone-api-service
  namespace: staging-gcp-kube
  labels:
    app: staging-gcp-kube-tone-api
    environment: staging-gcp-kube
spec:
  selector:
    app: staging-gcp-kube-tone-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
