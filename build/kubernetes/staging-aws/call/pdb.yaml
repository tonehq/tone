apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tone-inbound-call-worker-pdb
  namespace: staging
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: tone-inbound-call-worker
