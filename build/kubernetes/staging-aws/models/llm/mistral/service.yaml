apiVersion: v1
kind: Service
metadata:
  name: llm-mistral-service
  namespace: staging
  labels:
    app: llm-mistral
spec:
  selector:
    app: llm-mistral
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
