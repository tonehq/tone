apiVersion: v1
kind: Service
metadata:
  name: staging-llm-mistral-service
  namespace: staging
  labels:
    app: staging-llm-mistral
spec:
  selector:
    app: staging-llm-mistral
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
