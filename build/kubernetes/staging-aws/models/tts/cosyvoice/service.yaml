apiVersion: v1
kind: Service
metadata:
  name: tts-cosyvoice-service
  namespace: staging
  labels:
    app: tts-cosyvoice
spec:
  selector:
    app: tts-cosyvoice
  ports:
    - port: 80
      targetPort: 8188
      protocol: TCP
  type: ClusterIP
