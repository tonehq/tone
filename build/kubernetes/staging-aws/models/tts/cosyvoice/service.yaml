apiVersion: v1
kind: Service
metadata:
  name: staging-tts-cosyvoice-service
  namespace: staging
  labels:
    app: staging-tts-cosyvoice
spec:
  selector:
    app: staging-tts-cosyvoice
  ports:
    - port: 80
      targetPort: 8188
      protocol: TCP
  type: ClusterIP
