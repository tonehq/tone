apiVersion: v1
kind: Service
metadata:
  name: tts-qwen-service
  namespace: staging
  labels:
    app: tts-qwen
spec:
  selector:
    app: tts-qwen
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
