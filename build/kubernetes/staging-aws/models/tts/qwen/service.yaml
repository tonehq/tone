apiVersion: v1
kind: Service
metadata:
  name: staging-tts-qwen-service
  namespace: staging
  labels:
    app: staging-tts-qwen
spec:
  selector:
    app: staging-tts-qwen
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
