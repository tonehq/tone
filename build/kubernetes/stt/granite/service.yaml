apiVersion: v1
kind: Service
metadata:
  name: staging-stt-granite-service
  namespace: staging
  labels:
    app: staging-stt-granite
spec:
  type: ClusterIP
  selector:
    app: staging-stt-granite
  ports:
    - name: http
      port: 80
      targetPort: http
