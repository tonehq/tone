apiVersion: v1
kind: Service
metadata:
  name: staging-stt-voxtral-service
  namespace: staging
  labels:
    app: staging-stt-voxtral
spec:
  type: ClusterIP
  selector:
    app: staging-stt-voxtral
  ports:
    - name: http
      port: 80
      targetPort: http
