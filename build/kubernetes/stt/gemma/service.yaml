apiVersion: v1
kind: Service
metadata:
  name: staging-stt-gemma-service
  namespace: staging
  labels:
    app: staging-stt-gemma
spec:
  type: ClusterIP
  selector:
    app: staging-stt-gemma
  ports:
    - name: http
      port: 80
      targetPort: http
