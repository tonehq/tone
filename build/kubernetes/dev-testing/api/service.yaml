apiVersion: v1
kind: Service
metadata:
  name: dev-testing-tone-api-service
  namespace: dev-testing
  labels:
    app: dev-testing-tone-api
    environment: dev-testing
spec:
  selector:
    app: dev-testing-tone-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
