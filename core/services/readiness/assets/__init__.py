"""Bundled binary assets for readiness deep probes.

Currently holds the STT probe sample. Kept inside the ``core`` package so it
ships with wheels and stays discoverable via ``importlib.resources``.
"""
