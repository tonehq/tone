"use client";

import { Typography } from "antd";

const { Title } = Typography;

const HomePage = () => {
  return (
    <div>
       <Title level={4}>Home</Title>
    </div>
  );
};

export default HomePage;