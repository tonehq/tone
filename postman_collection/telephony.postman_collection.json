{
  "info": {
    "name": "Telephony API",
    "description": "Auto-generated from core/api/v1/telephony.py. WebSocket endpoints mounted at root level (not under /api/v1).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:8000" },
    { "key": "authToken", "value": "" }
  ],
  "auth": { "type": "noauth" },
  "item": [
    {
      "name": "Telephony WebSocket",
      "request": {
        "auth": { "type": "noauth" },
        "method": "GET",
        "header": [
          { "key": "Connection", "value": "Upgrade" },
          { "key": "Upgrade", "value": "websocket" }
        ],
        "url": {
          "raw": "{{baseUrl}}/ws",
          "host": ["{{baseUrl}}"],
          "path": ["ws"]
        },
        "description": "WebSocket endpoint for telephony providers (Twilio, Telnyx, Exotel, Plivo). Connect via ws://localhost:8000/ws. The server accepts the connection, parses the first telephony messages to identify the provider and call data, resolves the agent by the 'to' phone number, and runs the voice pipeline."
      },
      "response": [
        {
          "name": "Telephony WebSocket - Connection Info",
          "status": "Switching Protocols",
          "code": 101,
          "header": [
            { "key": "Connection", "value": "Upgrade" },
            { "key": "Upgrade", "value": "websocket" }
          ],
          "body": ""
        }
      ]
    },
    {
      "name": "Test WebSocket",
      "request": {
        "auth": { "type": "noauth" },
        "method": "GET",
        "header": [
          { "key": "Connection", "value": "Upgrade" },
          { "key": "Upgrade", "value": "websocket" }
        ],
        "url": {
          "raw": "{{baseUrl}}/ws/test?agent_id=1",
          "host": ["{{baseUrl}}"],
          "path": ["ws", "test"],
          "query": [
            { "key": "agent_id", "value": "1", "description": "The agent ID to test with" },
            { "key": "phone_number", "value": "%2B1234567890", "description": "Alternative: phone number to resolve agent", "disabled": true }
          ]
        },
        "description": "Raw PCM WebSocket for pipeline testing. Send raw 16-bit PCM audio bytes (16kHz, mono), receive bot audio back. Connect via ws://localhost:8000/ws/test?agent_id=20 or ws://localhost:8000/ws/test?phone_number=%2B1234567890"
      },
      "response": [
        {
          "name": "Test WebSocket - Connection Info",
          "status": "Switching Protocols",
          "code": 101,
          "header": [
            { "key": "Connection", "value": "Upgrade" },
            { "key": "Upgrade", "value": "websocket" }
          ],
          "body": ""
        },
        {
          "name": "Test WebSocket - Missing Params",
          "status": "Bad Request",
          "code": 4000,
          "header": [],
          "body": "Pass agent_id or phone_number as query parameter"
        },
        {
          "name": "Test WebSocket - Agent Not Found",
          "status": "Not Found",
          "code": 4004,
          "header": [],
          "body": "Agent not found for agent_id=999"
        }
      ]
    }
  ]
}
